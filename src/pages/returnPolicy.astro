---
/**
 * /returnPolicy — Return Policy
 * Pure SSG: CMS content fetched at build time, zero client JS.
 */
import ContentPage from '@/layouts/ContentPage.astro';
---

<ContentPage
  slug="return-policy"
  fallbackTitle="Return Policy"
  fallbackDescription="Return policy for tickets purchased through SightseeingRoma.com."
  fallbackBannerImage="/images/return-policy-banner.jpg"
/>
